; vim: ft=gitconfig

[user]
  name = Sam Merrell
  email = merrell.sam@gmail.com

[core]
  editor = vim

[color]
  pager = true
  status = auto
  branch = auto
  diff = auto
  interactive = auto

[push]
  default = tracking

[merge]
  tool = p4merge

[mergetool "p4merge"]
  cmd = p4merge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
  trustExitCode = false
  keepTemporaries = false

[diff]
  tool = p4merge

[difftool "p4merge"]
  cmd = p4merge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"

[alias]
  # Work flows
  devlatest = !git checkout dev && git fetch origin && git merge origin/dev --ff-only
  devrebase = !git checkout dev && git fetch origin && git merge origin/dev --ff-only && git checkout @{-1} && git rebase dev
  devpush = !git checkout dev && git merge @{-1} --ff-only && git push

  # Github workflows
  upmaster = !git checkout master && git fetch upstream && git merge upstream/master --ff-only && git push origin HEAD
  uprebase = !git checkout master && git fetch upstream && git merge upstream/master --ff-only && git push origin HEAD &&  git checkout @{-1} && git rebase master

  fmerge = merge --ff-only
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
  lstat = log --stat --relative --abbrev-commit --date=local --pretty=format:'%C(white)%h %C(cyan)%cr %C(green)by %cn %Creset%n%C(yellow)%cd%n%s'
  peeps = !git log --pretty=format:%aN | sort | uniq -c | sort -rn
  stache = !git add -A && git stash save

[log]
  date = local

[github]
  user = smerrell
