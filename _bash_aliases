alias pd='pushd $1'
alias cdp='cd -'
alias ssha='. ~/bin/sshagent'

if [[ $platform == Darwin* ]]; then
  alias gvim='mvim'
  alias ls='ls -G'
elif [[ $platform == Linux* ]]; then
  alias grep='grep --color=auto'
fi
